# --- Core data handling ---
pandas>=2.0.0
numpy>=1.24.0

# NOTE: RSI/EMA/SMA are implemented directly in indicators.py using only
# pandas — no pandas-ta dependency. pandas-ta was removed from PyPI (Sept
# 2025) after an ownership transfer and community reports questioning it
# as a possible supply-chain risk. Don't re-add it or a "successor"
# package without checking its current trust status first.

# --- HTTP requests (Binance REST, Telegram, Discord, ntfy) ---
requests>=2.31.0

# --- Free, keyless-ish equity/FX/commodity data (used for XAU/USD) ---
yfinance>=0.2.40

# --- Env var management for local dev (.env file) ---
python-dotenv>=1.0.0

# --- Optional: only needed if you run the CONTINUOUS websocket mode ---
websocket-client>=1.7.0
